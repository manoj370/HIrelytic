
export const environment = {
  production: true,

  // FOR UAT

  // authUrl: `http://`+ window.location.hostname + `:8080` + `/`,
  // serviceUrl:  `http://`+ window.location.hostname + `:8080` + `/`,
  // jcrURL:  `http://`+ window.location.hostname + `:8080` + `/digital-lib-app/`,
  // elasticSearchUrl: 'http://192.168.1.11:9200',
  // vendorUrl:`http://`+ window.location.hostname + `:8080` + `/vendor/`,
  // employeeUrl:`http://`+ window.location.hostname + `:8080` + `/referral/`



  // FOR LIVE AWS

  // authUrl: `https://` + window.location.hostname + '/',
  // serviceUrl: `https://` + window.location.hostname + '/',
  // jcrURL: `https://` + window.location.hostname + `/digital-lib-app/`,
  // elasticSearchUrl: 'https://search.hirelytic.com',
  // vendorUrl: `https://` + window.location.hostname + '/vendor/',
  //  employeeUrl: `https://` + window.location.hostname  + `/referral-management/`


};

